// Simple Basic Routing
const Login = () => {
  return (
    <div>
      <h1>Simple Basic Routing !!</h1>
    </div>
  );
};

export default Login;

/* 
Linking -> (using anchor tag) , Use on actions that wont't affect the website at all .
Navigation -> (using buttons), Use on actions that affects the websites frontend and the backened .
*/
