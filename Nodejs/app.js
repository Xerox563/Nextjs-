const os = require("os");
console.log(os.platform);

// Nodejs is a free open source js run time that runs on windows , Mac .
// Lets You execute the js code outside the browser, enabling the server side dev with js
// Built on Chrome V8 engine

// Nodejs uses ashynchronous(non blocking programming ) means it can handle multiple tasks like reading or talking to a db at once .
// Nodejs files run tasks on the server.
